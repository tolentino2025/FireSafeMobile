Fix the UI/UX of the "Fire Pump Annual Performance Test" mobile screen.

CURRENT PROBLEM:
The screen was generated as an accordion/list of dropdown sections (collapsible items). I do NOT want this.

TARGET UI (REQUIRED):
1) Replace the accordion/collapsible UI with a SINGLE SCROLLABLE SCREEN (vertical scroll).
2) All sections must be expanded by default (no collapsible dropdown sections).
3) Use a clean “form scroll” layout with:
   - Section headers (sticky optional)
   - Cards/blocks per section
   - Proper spacing and dividers
4) Keep the NFPA 25 field labels EXACTLY in English (do not translate or rename the form fields).

WHAT MUST BE DROPDOWN (pre-registered selections):
These are the ONLY things that should be dropdown-based selections:
- Fire Pump (pre-registered pumps)
- Contractor Company (pre-registered contractors)
- Inspector (pre-registered inspectors)
- Owner/Property Owner (pre-registered owners)
- Contracting Party / Client (pre-registered contracting parties / customers)
- Job/Site (pre-registered job sites)

When selected, auto-fill related fields, but allow override ONLY for this test record (do not overwrite master data unless an explicit “Update Master Record” button is used).

WHAT MUST NOT BE DROPDOWN:
- Do NOT turn the form sections into dropdown/accordion items.
- Do NOT hide content behind collapsible arrows.
- Do NOT force the user to open/close sections to fill the test.

SECTION BEHAVIOR (inside the scroll screen):
- Each main section is a visible card with its fields shown.
- For big tables (performance test tables), keep them as:
  a) horizontally scrollable table inside the section, OR
  b) a “row-by-row” input builder (0%, 25%, 50%, 75%, 100%, 125%, 150%, 0%, 100%, 150%) with a compact grid for columns
  Choose the most usable approach on mobile, but keep ALL NFPA required columns and rows.

OPTIONAL “SELECT ITEMS WITHIN SECTION” (allowed):
Inside some sections, if there are optional sub-items (e.g., multiple pump operation / transfer switch / attachments), allow:
- toggles/checkboxes to show/hide OPTIONAL blocks
BUT the main section itself must remain visible in the scroll (not collapsible).

NAVIGATION:
- Top app bar with back button and title “Performance Test”
- Add a sticky bottom action bar:
  - Save Draft
  - Submit / Finalize
  - Export PDF (if implemented)
- Add an in-page quick navigation (optional):
  - A small “Jump to Section” button opening a modal list of sections (NOT accordion). Clicking scrolls to that section.

DATA + UX REQUIREMENTS:
- Autosave draft (debounced)
- Validate required dropdowns before finalize (Fire Pump, Contractor, Inspector, Job/Site at minimum)
- Show a summary header at the top after selections:
  - Selected Pump
  - Selected Contractor
  - Selected Inspector
  - Selected Job/Site
- Keep everything mobile-first and fast.

DO NOT CHANGE:
- Database schema unless strictly necessary
- Field names/labels from the NFPA form
- The “Diesel vs Electric” split logic (still separate icons/modules)
- The requirement that master records are not overwritten without explicit action.

DELIVERABLE:
Implement the new scroll-based screen UI and remove the accordion dropdown sections entirely.
